diff --git a/dist/sandbox/sandbox-manager.js b/dist/sandbox/sandbox-manager.js
index 0b0d741ddf4688e843ff461f840edf76bad3ca10..ff2a799a20480d0247328055c4e06623ad710c48 100644
--- a/dist/sandbox/sandbox-manager.js
+++ b/dist/sandbox/sandbox-manager.js
@@ -716,8 +716,12 @@ function getFsWriteConfig() {
         }
         return true;
     });
-    // Build allowOnly list: default paths + configured allow paths
-    const allowOnly = [...getDefaultWritePaths(), ...allowPaths];
+    // Consult's strict adapter opts out because host-derived compatibility
+    // paths can be re-exposed through a broad denyRead policy on Linux.
+    const defaultWritePaths = config.filesystem.includeDefaultWritePaths === false
+        ? []
+        : getDefaultWritePaths();
+    const allowOnly = [...defaultWritePaths, ...allowPaths];
     return {
         allowOnly,
         denyWithinAllow: denyPaths,
